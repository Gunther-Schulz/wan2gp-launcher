name: wan2gp
channels:
  - conda-forge
  - nvidia
  - defaults
dependencies:
  - python=3.10.9  # Compatible with SageAttention 2.2.0 (>= 3.9)
  - pip
  - ffmpeg
  - git
  - cuda-toolkit=12.8  # CUDA toolkit for SageAttention compilation
