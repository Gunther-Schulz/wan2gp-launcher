name: wan2gp
channels:
  - conda-forge
  - nvidia
  - defaults
dependencies:
  - python=3.13  # Required for SageAttention3 (>= 3.13)
  - pip
  - ffmpeg
  - git
  - cuda-toolkit=12.8  # CUDA toolkit for SageAttention compilation
